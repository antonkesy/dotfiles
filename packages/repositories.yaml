package:
  hint: "Ubuntu apt repositories"
  ubuntu:
    - "sudo apt update -y"
    - "sudo apt install -y software-properties-common"
    - "sudo add-apt-repository -y universe"
    - "sudo add-apt-repository -y multiverse"
    - "sudo add-apt-repository -y restricted"
