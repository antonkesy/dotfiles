package:
  hint: "Ollama AI tool"
  depends_on:
    - aur-helpers
  ubuntu:
    - "curl -fsSL https://ollama.com/install.sh | sh"
  arch:
    - "yay -S --noconfirm ollama"
    - "sudo pacman -S --noconfirm ollama-cuda"
