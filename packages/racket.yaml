package:
  hint: "Racket programming language"
  depends_on:
    - snap
  ubuntu:
    - "sudo snap install racket"
    - "sudo apt install -y graphviz"
  arch:
    - "sudo pacman -S --noconfirm --needed racket graphviz"
