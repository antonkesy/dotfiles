package:
  hint: "VPN dependencies for Cisco AnyConnect"
  ubuntu:
    - "sudo add-apt-repository deb http://gb.archive.ubuntu.com/ubuntu jammy main -y"
    - "sudo apt update"
    - "sudo apt install -y libwebkit2gtk-4.0-dev"
  arch:
    - "yay -S --noconfirm openconnect networkmanager-openconnect"
