#!/bin/bash
# all small packages without grouping

sudo apt install -y ffmpeg flex pandoc pdfarranger thunderbird libreoffice gimp baobab curl qt6-base-dev anki python3-distutils latexmk texstudio texlive-full ubuntu-restricted-extras
sudo snap install krita postman obs-studio
pip install pre-commit

sudo apt-get install exfat-fuse ntfs-3g -y # disk format support
sudo apt install flatpak
