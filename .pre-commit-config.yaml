# grep path .gitmodules | sed 's/^[ \t]*path = //' | paste -sd '|' -
# exclude auto-generated settings file "/home/.config/DankMaterialShell/settings.json"
exclude: ^(home/.config/nvim|home/.config/alacritty/themes|home/.tmux/plugins/tpm|home/keypocalypse-now|home/.config/DankMaterialShell/settings.json)
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-json
      - id: check-merge-conflict
      - id: detect-private-key
  - repo: https://github.com/koalaman/shellcheck-precommit
    rev: v0.11.0
    hooks:
      - id: shellcheck
        args: ["--shell=bash", "--severity=error"]
        exclude: ^(home/.config/zsh/p10k.zsh)
  - repo: https://github.com/zricethezav/gitleaks
    rev: v8.30.0
    hooks:
      - id: gitleaks
  - repo: https://github.com/scop/pre-commit-shfmt
    rev: v3.13.1-1
    hooks:
      - id: shfmt
  - repo: https://github.com/google/yamlfmt
    rev: v0.21.0
    hooks:
      - id: yamlfmt
  - repo: https://github.com/adrienverge/yamllint
    rev: v1.38.0
    hooks:
      - id: yamllint
        args:
          - --strict
          - -d
          - |
            {
              extends: default,
              rules: {
                line-length: {max: 150},
                document-start: disable,
                truthy: {check-keys: false},
                comments: {min-spaces-from-content: 1}
              }
            }
