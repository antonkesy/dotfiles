# Boot logs stay on the console until the greeter is about to draw; after that
# kernel warnings and systemd status lines would overwrite tuigreet, so send
# them to the journal only.
[Service]
ExecStartPre=-/usr/bin/kill -s RTMIN+21 1
ExecStartPre=-/usr/bin/dmesg -n 1
